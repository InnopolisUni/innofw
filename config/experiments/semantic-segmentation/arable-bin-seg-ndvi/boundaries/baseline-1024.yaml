# @package _global_
defaults:
  - _self_
  - override /models: semantic-segmentation/deeplabv3plus
  - override /datasets: semantic-segmentation/arable-bin-seg-ndvi/boundaries/tile-1024-channels-459.yaml
  - override /loggers: wandb
  - override /losses: semantic-segmentation/dice
  - override /optimizers: adam
  - override /augmentations_train: arable-ndvi
  - override /augmentations_val: arable-ndvi

  - override /wandb: default

datasets:
  num_workers: 32

augmentations_train:
  augmentations:
    position:
      random_crop:
        height: 896
        width: 896

augmentations_val:
  augmentations:
    position:
      random_crop:
        height: 896
        width: 896

models:
  in_channels: 3
  classes: 1
  encoder_name: dpn98
  encoder_depth: 5
  encoder_weights:


project: arable-bound-bin-seg-ndvi
task: image-segmentation
random_seed: 42
accelerator: gpu
devices: 1
batch_size: 40
num_workers: 32
weights_freq: 15
epochs: 200

loggers:
  project: arable-bound-bin-seg-ndvi

wandb:
  entity: qazybi
  enable: True
  project: arable-bound-bin-seg-ndvi
  group: baseline
  job_type: training
