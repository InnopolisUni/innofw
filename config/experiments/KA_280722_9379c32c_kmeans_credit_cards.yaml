# @package _global_
defaults:
  - override /models: kmeans
  - override /datasets: clustering_credit_cards
  - override /callbacks: clustering


project: "credit_cards"
task: "table-clustering"
random_seed: 42
