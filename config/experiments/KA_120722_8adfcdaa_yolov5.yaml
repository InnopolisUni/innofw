# @package _global_
defaults:
  - override /models: yolov5
  - override /datasets: detection_lep
  - override /metrics: detection
  - override /optimizers: sgd

project: "lep"
task: "image-detection"
random_seed: 42
epochs: 2
batch_size: 2
weights_freq: 1
