# @package _global_
defaults:
  - override /models: detection/flor
  - override /datasets: detection/flor
#  - override /optimizers: sgd

project: "flor"
task: "image-detection"
random_seed: 42
epochs: 40
batch_size: 16
weights_freq: 1
ckpt_path: "flor_files/florence2_epoch_136.onnx"
