# @package _global_
defaults:
  - override /models: detection/yolov5
  - override /datasets: detection/air_data
  - override /optimizers: sgd

project: "air_data"
task: "image-detection"
random_seed: 42
epochs: 2
batch_size: 2
weights_freq: 1
