# @package _global_
defaults:
  - override /models: deeplabv3plus.yaml
  - override /datasets: segmentation_landslides
  - override /optimizers: adam.yaml
  - override /losses: segmentation_losses
  - override /augmentations: none #segmentation_satellite_imagery  # _inference
  # - override /augmentations_train: segmentation_medium
  - override /initializations: he
  - override /callbacks: segmentation

project: "segmentation"
task: "image-segmentation"

random_seed: 42
weights_freq: 1
batch_size: 16
epochs: 50
accelerator: gpu
gpus: [2]
models:
  in_channels: 4
